---
phase: 6
plan: 3
wave: 2
---

# Plan 6.3: Audit, Build & Final Launch

## Objective
Thực hiện thẩm định (Audit) lại toàn bộ mã nguồn, build thành bản Production chất lượng cao nhất và thiết lập quy trình tự động hóa triển khai (CI/CD) lên GitHub Pages.

## Context
- .gsd/phases/6/RESEARCH.md
- vite.config.ts
- package.json
- .github/workflows/deploy.yml

## Tasks

<task type="auto">
  <name>Final Production Build Audit</name>
  <files>package.json, vite.config.ts</files>
  <action>
    - Chạy `npm run build` để kiểm tra lỗi build bản chính thức.
    - Cấu hình Vite Base path phù hợp nếu deploy lên GitHub Pages (`/portfolio/`).
    - Audit kích thước mã nguồn sau bundle.
  </action>
  <verify>Check `dist/` directory and build logs.</verify>
  <done>Mã nguồn bản Production lỗi (Error-free) và được tối ưu hóa (Minified/Otimized).</done>
</task>

<task type="auto">
  <name>Setup GitHub Actions Deploy Workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
    - Tạo workflow file: Sử dụng `actions/checkout`, `actions/setup-node`, và `peaceiris/actions-gh-pages` (hoặc tương đương).
    - Tự động Build mỗi khi push/merge lên nhánh `main`.
    - Deploy tự động kết quả build lên nhánh `gh-pages` để website LIVE.
  </action>
  <verify>Push commits to main and monitor GitHub Actions status.</verify>
  <done>Website tự động cập nhật mỗi khi mã nguồn có sự thay đổi (Continuous Deployment).</done>
</task>

## Success Criteria
- [ ] Website LIVE (truy cập được qua link gh-pages).
- [ ] Lighthouse Performance score >90.
- [ ] Toàn bộ Roadmp - v1.0 COMPLETE (Phase 1-6).
